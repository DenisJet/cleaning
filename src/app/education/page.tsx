import styles from './page.module.css';

export default function Education() {
  return (
    <main className={styles.main}>
      <section className={styles.about}>
        <h1 className={styles.title}>Тут будет Обучение</h1>
      </section>
    </main>
  );
}
